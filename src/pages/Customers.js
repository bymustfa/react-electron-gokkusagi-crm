import React from "react";
import { Layout, CardHeader } from "../components/partials";

export default function Customers(props) {
  return (
    <Layout>
      <CardHeader title="Müşteriler" />
    </Layout>
  );
}
